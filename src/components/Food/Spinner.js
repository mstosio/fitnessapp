import React, { useState} from 'react';
import { StyledSpinner} from '../../layout/styledFood/index';


const Spinner = props => {
    return (
        <StyledSpinner>Hello</StyledSpinner>
    );
};

export default Spinner;